# System-Related Parameters
ANDROID_DATA=/data
ANDROID_ROOT=/system
SHELL=/bin/bash               # Replace with /usr/bin/zsh or your preferred shell
PREFIX=/usr/local             # Default prefix, modify only for custom setups
HOME=/home/yourusername       # Automatically set, no need to modify

# Application-Specific Parameters
XDG_CONFIG_DIRS=/etc/xdg
XDG_DATA_HOME=~/.khkhklocal/share
XDG_CONFIG_HOME=~/.config
XDG_CACHE_HOME=~/.cache
XDG_STATE_HOME=~/.local/state

# Databricks or Cloud Environment Parameters
DATABRICKS_RUNTIME_VERSION=10.x  # Modify if applicable to your Databricks setup

# Framework-Specific Parameters
PYDANTIC_DISABLE_PLUGINS=false   # Set to true only if plugins need to be disabled